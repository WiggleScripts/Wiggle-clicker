local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local lp = Players.LocalPlayer
local playerGui = lp:WaitForChild("PlayerGui")

local autoFarming = false
local IMAGE_ID = "rbxassetid://101682020950269"

local function forceClick(button)
    local signals = {"MouseButton1Click", "MouseButton1Down", "Activated", "TouchTap"}
    for _, sigName in pairs(signals) do
        local sig = button[sigName]
        if sig then 
            pcall(function()
                for _, con in pairs(getconnections(sig)) do con:Fire() end 
            end)
        end
    end
end

local function makeDraggable(obj, screenGui)
    local dragging, dragStart, startPos
    obj.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true dragStart = input.Position startPos = obj.AbsolutePosition
        end
    end)
    UIS.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            local screen = screenGui.AbsoluteSize
            local objSize = obj.AbsoluteSize
            local newX = math.clamp(startPos.X + delta.X, 0, screen.X - objSize.X)
            local newY = math.clamp(startPos.Y + delta.Y, 0, screen.Y - objSize.Y)
            obj.Position = UDim2.new(0, newX, 0, newY)
        end
    end)
    UIS.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end
    end)
end

if playerGui:FindFirstChild("WiggleClicker") then playerGui.WiggleClicker:Destroy() end
local gui = Instance.new("ScreenGui", playerGui)
gui.Name = "WiggleClicker"
gui.ResetOnSpawn = false

local mainFrame = Instance.new("Frame", gui)
mainFrame.Size = UDim2.new(0, 300, 0, 250)
mainFrame.Position = UDim2.new(0.5, -150, 0.5, -125)
mainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
mainFrame.Visible = false
Instance.new("UICorner", mainFrame)

local bgImg = Instance.new("ImageLabel", mainFrame)
bgImg.Size = UDim2.new(1, 0, 1, 0)
bgImg.Image = IMAGE_ID
bgImg.ImageTransparency = 0.8
bgImg.BackgroundTransparency = 1
bgImg.ScaleType = Enum.ScaleType.Crop
Instance.new("UICorner", bgImg)

local title = Instance.new("TextLabel", mainFrame)
title.Size = UDim2.new(1, 0, 0, 50)
title.Text = "WIGGLE CLICKER V9.9.1"
title.Font = Enum.Font.GothamBlack
title.TextColor3 = Color3.fromRGB(255, 215, 0)
title.BackgroundTransparency = 1
title.TextSize = 18
title.ZIndex = 2

local scroll = Instance.new("ScrollingFrame", mainFrame)
scroll.Size = UDim2.new(1, -20, 1, -90)
scroll.Position = UDim2.new(0, 10, 0, 55)
scroll.BackgroundTransparency = 1
scroll.CanvasSize = UDim2.new(0, 0, 0, 220)
scroll.ScrollBarThickness = 2
scroll.ZIndex = 2

local layout = Instance.new("UIListLayout", scroll)
layout.Padding = UDim.new(0, 8)
layout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local function makeBtn(txt, callback)
    local b = Instance.new("TextButton", scroll)
    b.Size = UDim2.new(0, 250, 0, 55)
    b.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    b.Text = txt
    b.Font = Enum.Font.GothamBold
    b.TextColor3 = Color3.new(1, 1, 1)
    b.TextSize = 15
    b.ZIndex = 3
    Instance.new("UICorner", b)
    local s = Instance.new("UIStroke", b)
    s.Color = Color3.fromRGB(255, 215, 0)
    s.Thickness = 2
    b.MouseButton1Click:Connect(function() callback(b, s) end)
end

makeBtn("START CLICKER", function(btn, strk)
    autoFarming = not autoFarming
    btn.Text = autoFarming and "CLICKING: ON" or "START CLICKER"
    strk.Color = autoFarming and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 215, 0)
end)

local note = Instance.new("TextLabel", scroll)
note.Size = UDim2.new(0, 250, 0, 30)
note.BackgroundTransparency = 1
note.Text = "*Only works when morphed into a monster"
note.Font = Enum.Font.GothamItalic
note.TextColor3 = Color3.fromRGB(200, 200, 200)
note.TextSize = 11
note.ZIndex = 3

local openBtn = Instance.new("ImageButton", gui)
openBtn.Size = UDim2.new(0, 65, 0, 65)
openBtn.Position = UDim2.new(0.05, 0, 0.45, 0)
openBtn.Image = IMAGE_ID
openBtn.BackgroundColor3 = Color3.fromRGB(255, 215, 0)
Instance.new("UICorner", openBtn)

local hide = Instance.new("TextButton", mainFrame)
hide.Size = UDim2.new(0, 80, 0, 30)
hide.Position = UDim2.new(0.5, -40, 0.88, 0)
hide.Text = "CLOSE"
hide.Font = Enum.Font.GothamBold
hide.TextColor3 = Color3.new(0.7, 0.7, 0.7)
hide.BackgroundTransparency = 1
hide.ZIndex = 3

makeDraggable(mainFrame, gui)
makeDraggable(openBtn, gui)

openBtn.MouseButton1Click:Connect(function() mainFrame.Visible = true openBtn.Visible = false end)
hide.MouseButton1Click:Connect(function() mainFrame.Visible = false openBtn.Visible = true end)

task.spawn(function()
    while true do
        task.wait(0.1)
        if autoFarming then
            for _, v in pairs(playerGui:GetDescendants()) do
                if (v:IsA("TextButton") or v:IsA("ImageButton") or v:IsA("TextLabel")) and v.Visible then
                    if v.Name:lower():find("track") or (v:IsA("TextLabel") and v.Text:lower():find("track")) then
                        local t = v:IsA("TextLabel") and (v:FindFirstAncestorWhichIsA("GuiButton") or v.Parent) or v
                        if t:IsA("GuiButton") then forceClick(t) end
                    end
                end
            end
        end
    end
end)
